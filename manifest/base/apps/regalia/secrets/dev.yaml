# 保管庫へ実値を登録し、認証 Secret を投入してから有効化する。
enabled: false
secrets:
  - name: discord-oauth-client
    keys:
      client-id: scoreserver-discord-client-id
      client-secret: scoreserver-discord-client-secret
