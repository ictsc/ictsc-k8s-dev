# 既存値のインポートと認証 Secret の投入後に有効化する。
enabled: false
secrets:
  - name: oidc
    keys:
      client-id: oauth2-proxy-client-id
      client-secret: oidc-oauth2-proxy-client-secret
      cookie-secret: oauth2-proxy-cookie-secret
